<h1>Services</h1>
<%= simple_form_for :search, url: root_path, method: :get do |f| %>
  <div class="search-form-control form-group">
    <input class="form-control string required" type="text" name="search[query]" id="search_query" />
    <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> Search
    </button>
  </div>
<% end %>


<div class="card-container">
  <% @services.each do |service| %>
      <div class="card-service">
        <img src="<%= service.video_url %>" />
        <%# how to pass the url for the video????? !!!!! %>
      <div class="card-service-infos">
        <div>
          <h2><%= service.part %></h2>
          <p><%= service.description %></p>
        </div>
      <h2 class="card-service">٭٭٭٭</h2>
      <img src="https://kitt.lewagon.com/placeholder/users/krokrob"  class="card-service-user avatar-bordered" />
    </div>
  </div>
  <% end %>
</div>

